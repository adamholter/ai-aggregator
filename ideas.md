# Product Ideas (Approved)

This document captures the product features you approved for implementation next. It is intentionally specific enough to guide end-to-end work in this repo.

---

## All Previously Listed Ideas Have Been Implemented ✅

The following features have been completed and deployed:

1. **Command Palette + Hotkeys** - `Ctrl+K`/`Cmd+K` opens palette, `?` for help
2. **Unified Model Identity + Persisted Cross-Source Matching** - LLM matching between AA↔OpenRouter with `data/model_matches.json` storage + new LLM-based model card lookup for image models
3. **Local Notes (and Tags) on Cards** - Notes stored in localStorage, visible in model modals
4. **Shareable Deep Links (Stateful URLs)** - 🔗 Share button encodes tab, search, sort, compare tray in URL
5. **Export Everywhere** - 📥 Export dropdown with JSON/CSV/Pinned/Compare options

---

## Add New Ideas Below

Future product ideas can be added here for prioritization and implementation.
