{
  "name": "ai-dashboard-flask-ui-tests",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "data:refresh": "node scripts/scrape_deepswe_leaderboard.mjs && node scripts/build_benchmark_warehouse.mjs",
    "build": "npm run data:refresh && echo 'Refreshed live benchmark data; no frontend compilation required'",
    "test:benchmarks": "node --test tests/deepswe-identity.test.mjs",
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.2"
  },
  "dependencies": {
    "@openrouter/agent": "^0.7.2",
    "zod": "^4.4.3"
  }
}
